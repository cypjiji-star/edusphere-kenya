rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are completely open for debugging purposes.
    // They should be secured before deploying the application.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
